<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.optimalsolutions">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OptimalSolutions">
        <activity
            android:name=".FaultReport"
            android:exported="true" />
        <activity
            android:name=".ui.login.Fault_report"
            android:exported="true"
            android:label="@string/title_activity_fault_report" />
        <activity android:name=".Ticket1" />
        <activity android:name=".NewTicketActivity" />
        <activity android:name=".ExistingTicketActivity" />
        <activity android:name=".TicketsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.OptimalSolutions.NoActionBar" />
    </application>

</manifest>